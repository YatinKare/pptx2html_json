import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
import { ToolDefinition, ToolExecuteOptions } from '@sylphlab/tools-core';

type McpToolDefinition = ToolDefinition<any, any>;
interface McpServerOptions {
    name: string;
    version: string;
    description: string;
    tools: McpToolDefinition[];
}
declare function registerTools(server: McpServer, tools: McpToolDefinition[], toolOptions: ToolExecuteOptions): void;
declare function startMcpServer(serverOptions: McpServerOptions, toolOptions: ToolExecuteOptions): Promise<McpServer>;

export { type McpServerOptions, type McpToolDefinition, registerTools, startMcpServer };
